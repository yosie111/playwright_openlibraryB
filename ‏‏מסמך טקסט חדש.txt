כעת הוסף פונקציה async def measure_page_performance(page, url: str, threshold_ms: int) -> dict:. מדידת ביצועים – Performance (45–60 דקות)

async def measure_page_performance(page, url: str, threshold_ms: int) -> dict:

 

•	מדידת load_time_ms, dom_content_loaded_ms, first_paint_ms.

•	חריגה מ-threshold = אזהרה בלוג (לא כישלון).

•	הפקת performance_report.json בסיום.

נקודות מדידה: עמוד חיפוש (3000ms) | עמוד ספר (2500ms) | Reading List (2000ms)

                                  •         דוחות: Allure Reports / HTML + Screenshots Performance (מדידות, threshold, דוח JSON)